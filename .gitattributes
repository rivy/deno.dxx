# default; use LF EOLs for text files
* text=auto eol=lf

# (CRLF required) * WinOS BAT/CMD and MSVC SLN files require CRLF EOLs
*.[bB][aA][tT]    text eol=crlf
*.[cC][mM][dD]    text eol=crlf
*.[sS][lL][nN]    text eol=crlf
